<h3><%= @appointment.start_date %> - <%= @appointment.end_date %></h3>
<%= link_to("Edit", edit_appointment_path(@appointment)) %>
<%= link_to("Delete", appointment_path(@appointment), data: {method: :delete, confirm: "Are you sure you want to delete this appointment?"}) %>

<h1><%= @appointment.start_date %> - <%= @appointment.end_date %></h1>

<%= link_to("Edit Appointment", edit_appointment_path(@appointment)) %>
<%= link_to("Delete Appointment", appointment_path(@appointment), data: {method: :delete, confirm: "Are you sure you want to delete this appointment?"}) %>